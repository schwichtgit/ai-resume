name: Security Scan

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1' # Weekly Monday 6am UTC

permissions:
  contents: read
  security-events: write

jobs:
  container-scan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - context: frontend
            image: ai-resume-frontend
          - context: api-service
            image: ai-resume-api
          - context: memvid-service
            image: ai-resume-memvid
          - context: ingest
            image: ai-resume-ingest
    steps:
      - uses: actions/checkout@v6

      - name: Prepare build context
        if: matrix.context == 'memvid-service'
        run: cp -r proto/ memvid-service/proto/

      - name: Build image
        run: docker build -t ${{ matrix.image }}:scan ${{ matrix.context }}

      - name: Run Trivy vulnerability scanner (SARIF)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: '${{ matrix.image }}:scan'
          format: 'sarif'
          output: 'trivy-results.sarif'
          trivyignores: '.trivyignore'

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy-${{ matrix.image }}'

      - name: Trivy severity gate (CRITICAL/HIGH)
        uses: aquasecurity/trivy-action@v0.36.0
        env:
          TRIVY_SEVERITY: CRITICAL,HIGH
          TRIVY_IGNORE_UNFIXED: 'true'
        with:
          image-ref: '${{ matrix.image }}:scan'
          format: 'table'
          exit-code: '1'
          trivyignores: '.trivyignore'
