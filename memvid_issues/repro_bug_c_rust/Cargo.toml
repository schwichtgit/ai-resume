[package]
name = "repro-bug-c"
version = "0.1.0"
edition = "2021"
description = "Standalone reproduction of memvid issue #196 (time index frame id out of range)"

[dependencies]
memvid-core = { version = "2.0.137", features = ["lex"] }
